# PostgreSQL (use with DBeaver or any Postgres client)
# USER = your Postgres login (usually "postgres"). DATABASE = database name ("hris").
# Option 1: connection string
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@localhost:5432/hris

# Option 2: separate variables (defaults used if not set)
PGHOST=localhost
PGPORT=5432
PGDATABASE=hris
PGUSER=postgres
PGPASSWORD=postgres

# Server
PORT=3000
